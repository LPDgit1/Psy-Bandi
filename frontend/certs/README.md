Local CA certificates used only by Docker builds on this workstation.

Place public root CA certificates with the `.crt` extension here when a local
security product intercepts HTTPS traffic. Do not add private keys or client
certificates.
